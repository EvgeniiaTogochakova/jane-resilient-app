import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4E5E07',
    },
    secondary: {
      main: '#E19D29',
    },

    action: {
      active: '#4C2F27',
    },
  },
});

export default theme;
